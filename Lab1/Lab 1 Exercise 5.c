#include <stdio.h>
int main()
{
    char name[30],branch[15],uni[40],add[40];
    puts("Enter Name:");
    gets(name);
    puts("\nEnter Branch:");
    gets(branch);
    puts("\nEnter University Name:");
    gets(uni);
    puts("\nEnter Address Of University");
    gets(add);
    puts("\n");
    puts(name);
    puts(branch);
    puts(uni);
    puts(add);

    return 0;
    }
